# Custom 分支开发规则

本文档约束本仓库的二次开发、上游同步、冲突处理和发布流程。凡在 `custom` 或 `feature/*` 分支上进行开发、审查、同步或发布，均应遵守本规则。

## 1. 分支职责

- `main` 是上游同步分支，只允许同步 `upstream/main`，不承载任何定制代码。
- `custom` 是定制开发主分支，所有二次开发最终合并到 `custom`。
- `feature/*` 是短期功能分支，必须从最新的 `custom` 创建，并在功能完成后合并回 `custom`。
- Hotfix 应从 `custom` 或对应的 `custom` 发布标签创建，修复完成后必须回流到 `custom`。
- 禁止将 `custom`、`feature/*`、Hotfix 或任何定制提交合并、Cherry-pick 或 Rebase 到 `main`。
- 分支流向固定为：`upstream/main -> main -> custom -> feature/* -> custom`。不得反向传播定制提交。

## 2. 上游同步

同步上游时必须先更新 `main`，再将更新带入 `custom`：

1. 获取 `upstream` 最新引用。
2. 确认本地 `main` 没有定制提交，并以 Fast-forward 方式更新到 `upstream/main`；不得在 `main` 上生成额外 Merge Commit。
3. 切换到 `custom`，确认工作区干净。
4. Rebase 前创建明确命名的备份分支，例如 `backup/custom-before-rebase-<timestamp>`。
5. 个人维护 `custom` 时，优先将 `custom` Rebase 到最新 `main`。
6. Rebase 完成并验证后，推送改写后的历史必须使用 `--force-with-lease`，禁止使用裸 `--force`。

补充约束：

- Rebase 前先确认远端 `custom` 是否出现其他维护者的新提交，避免覆盖他人工作。
- 多人共享 `custom` 时，改写远端历史前必须先协调并暂停其他推送；无法协调时不得擅自 Rebase 已共享的历史。
- 上游同步与业务开发应分开处理，不要在同一次提交中混入定制功能、依赖升级或格式化改动。
- `upstream` Remote 应仅用于跟踪原项目；向上游提交变更必须走独立的上游贡献流程，不得夹带本仓库定制逻辑。

## 3. 二次开发原则

- 用户明确限定修改范围时，必须严格遵守；未被点名的文件一律不得修改。代码任务默认只修改完成需求所必需的代码文件，除非用户当前消息明确要求，否则禁止修改 `CHANGELOG.md`、`README.md`、`docs/**` 等文档文件，不得以文档规范、惯例或“顺手补充”为由扩大改动范围。
- 每次任务完成前必须检查变更文件列表，确认没有超出用户指定范围；若发现自动化工具造成超范围改动，必须立即撤销该工具自身的改动，但不得触碰用户已有改动。
- 优先新增独立文件、模块、Adapter、Middleware、Hook、插件或配置扩展点，减少直接修改上游核心文件。
- 必须修改上游文件时，只保留实现当前需求所需的最小改动，不进行无关格式化、重命名、移动或大范围重构。
- 每个提交只包含一个明确目的；不要把依赖升级、格式化、生成文件更新和业务改动混在同一提交中。
- 定制逻辑应集中在边界清晰的目录或模块中，避免散落到多个上游文件。
- 对上游文件的修改应使用清晰的提交信息，说明定制目的和影响范围，便于 Rebase 时逐个识别和解决冲突。
- 定制功能应尽量通过配置或 Feature Flag 控制，避免改变上游默认行为；除非发布需求明确要求，否则新增开关默认不得影响未启用场景。
- 新增依赖前先确认现有依赖无法满足需求。依赖变更及对应 Lockfile 更新应保持聚焦，不得包含无关依赖刷新。
- 数据库 Schema 或迁移必须与对应功能放在同一功能分支中，并明确升级、回滚和与上游迁移的顺序；不得静默修改已发布的迁移文件。
- 生成文件只在确有需要时更新，并确保可以追溯到对应源码或生成命令，避免无关的大面积变更。
- 密钥、Token、生产地址和个人环境配置不得提交到仓库；定制配置应通过环境变量、示例配置或受控配置文件提供。

## 4. 冲突处理

- 发生冲突时，先理解上游变更的目的和新实现，再基于最新上游代码重新应用定制逻辑。
- 不得简单选择一侧覆盖另一侧，也不得使用 `merge=ours`、批量 Accept Ours/Accept Theirs 等方式静默忽略同一文件中的上游更新。
- 冲突解决应保持上游新行为，并重新确认定制需求是否仍然成立；上游已提供等价能力时，应优先删除重复定制。
- 解决冲突后，重点检查受影响功能、公共 API、配置、依赖、数据库迁移、生成文件和前端构建。
- 对难以判断的冲突，应拆分处理并记录选择理由，不得为了完成 Rebase 而保留无法解释的代码。
- Rebase 完成后先比较备份分支与新 `custom` 的提交及文件差异，确认没有定制提交丢失，再删除备份分支。

## 5. 提交与审查

- 功能开发应在 `feature/*` 分支完成，不直接在 `main` 上开发；除紧急且明确约定的情况外，也不直接在 `custom` 堆叠未完成改动。
- 提交信息应能区分“上游同步”“定制功能”“冲突修复”“依赖调整”“文档调整”等目的。
- 合并到 `custom` 前，应检查改动范围是否包含无关文件、上游核心文件是否可以减少修改，以及 Feature Flag 是否保持默认行为。
- Pull Request 的目标分支必须与职责匹配：定制功能以 `custom` 为目标，上游贡献使用独立分支和独立流程，不以本仓库 `main` 承载。
- 不得通过 Squash 掩盖无法解释的上游文件改动；是否 Squash 应以保留清晰、可 Rebase 的历史为准。

## 6. 验证要求

- 上游同步或冲突解决后，应优先验证实际受影响的范围，不以“Rebase 成功”代替功能验证。
- 至少检查与改动相关的类型检查、测试、构建或手工流程；若某项未执行，应在合并或发布说明中明确标注。
- 涉及 API、数据库迁移、配置格式或构建流程时，应同时验证升级路径以及未启用定制功能时的默认行为。
- CI 应覆盖 `custom` 和 `feature/*` 的合并请求；不得仅以 `main` 的 CI 结果作为定制版本可发布的依据。

## 7. 发布基线

- 生产部署和测试环境必须明确使用 `custom`、基于 `custom` 创建的发布分支，或指向 `custom` 提交的发布标签；不得默认使用 `main`。
- 发布标签必须指向已验证的定制提交，不得在同一版本标签下移动或重写指向。
- 发布前确认当前提交同时包含目标上游基线和所需定制功能，并记录对应的上游基线提交，便于审计和后续同步。
- 发布前确认部署配置、数据库迁移、Feature Flag 和环境变量与目标环境一致。
- 每次发布应保留可回滚的提交或标签，并明确数据库或配置发生不可逆变化时的恢复方案。
- CI/CD 的默认部署来源应显式设置为 `custom` 或其发布标签，避免因默认分支配置而误部署 `main`。

## 8. 分支保护与仓库配置

- 建议保护 `main`，限制直接推送，只允许由 `upstream/main` 同步产生的 Fast-forward 更新。
- 建议保护 `custom`，要求通过 Pull Request 和必要检查后合并；确需 Rebase 并使用 `--force-with-lease` 更新时，应限制为维护者操作。
- 自动依赖更新、格式化机器人和发布工作流必须明确目标分支，避免自动向 `main` 注入定制提交或从 `main` 触发定制版本发布。
- 仓库默认分支、部署分支与上游同步分支是不同概念；修改相关平台设置时必须分别核对，不得依赖隐式默认值。

## 9. 操作检查清单

### 同步上游前

- [ ] 当前在 `main` 或 `custom` 的正确操作阶段。
- [ ] 工作区干净，未覆盖用户或其他维护者的未提交改动。
- [ ] `main` 不包含任何定制提交。
- [ ] 已获取最新的 `upstream/main` 和远端 `custom`。
- [ ] Rebase 前已创建 `custom` 备份分支。

### 同步上游后

- [ ] 已逐项处理冲突，并理解上游与定制两侧的修改目的。
- [ ] 已确认定制提交没有丢失、重复或意外进入 `main`。
- [ ] 已检查受影响的功能、API、配置、迁移和构建。
- [ ] 如需改写远端历史，仅使用 `--force-with-lease`。

### 发布前

- [ ] 发布提交来自 `custom` 或其发布分支/标签，而不是 `main`。
- [ ] 已确认包含目标上游基线和全部所需定制功能。
- [ ] 已核对 Feature Flag、环境变量、迁移和部署配置。
- [ ] 已保留可回滚提交或标签，并记录未执行的验证项与已知风险。
